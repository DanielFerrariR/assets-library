import { kpiData } from '@/fixtures/kpiData';
import { layoutData } from '@/fixtures/layoutData';
import { storyboardData } from '@/fixtures/storyboardData';

export const searchData = {
  data: [...kpiData, ...layoutData, ...storyboardData],
};
